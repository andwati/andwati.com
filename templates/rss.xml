<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="{{ get_url(path="rss.xsl", trailing_slash=false) }}"?>
<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" version="2.0">
    <channel>
      <title>{{ config.title }}</title>
      <link>{{ config.base_url }}</link>
      <description>{{ config.description }}</description>
      <generator>Zola</generator>
      <language>{{ config.default_language | default(value="en") }}</language>
      <atom:link href="{{ feed_url | safe }}" rel="self" type="application/rss+xml"/>
      <lastBuildDate>{{ last_updated | date(format="%a, %d %b %Y %H:%M:%S %z") }}</lastBuildDate>
      {% for page in pages %}
      <item>
          <title>{{ page.title }}</title>
          <pubDate>{{ page.date | date(format="%a, %d %b %Y %H:%M:%S %z") }}</pubDate>
          <author>{{ page.extra.author | default(value=config.extra.author) | default(value="Unknown") }}</author>
          <link>{{ page.permalink | safe }}</link>
          <guid>{{ page.permalink | safe }}</guid>
          <description xml:base="{{ page.permalink | safe }}">{{ page.description | default(value=config.description) }}</description>
          <content:encoded>{{ page.content }}</content:encoded>
      </item>
      {% endfor %}
    </channel>
</rss>
