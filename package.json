{
  "name": "andwati-com",
  "private": true,
  "devDependencies": {
    "wrangler": "^4"
  },
  "scripts": {
    "generate:llms": "node scripts/generate-llms.mjs",
    "build": "npm run generate:llms && zola build",
    "deploy": "npm run build && wrangler deploy"
  }
}
